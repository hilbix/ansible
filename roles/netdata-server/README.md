# `netdata-server`

Setup NetData receiver for `netdata-client`.

```
  roles:
    - role: netdata-server
```

Missing parts:

- `netdata-client`
- Configure retention
- Configure gateway to `rrdcached`

