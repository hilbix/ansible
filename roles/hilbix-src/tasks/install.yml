
- name: check {{ prog }} exists
  ansible.builtin.stat:
    path: /usr/local/bin/{{ prog }}
  register: item_stat

- name: compiling {{ prog }}
  ansible.builtin.shell:
    cmd: make -C src/{{ prog }}/
  when: not item_stat.stat.exists

- name: installing {{ prog }}
  ansible.builtin.shell:
    cmd: make -C src/{{ prog }}/ install
  become: true
  when: not item_stat.stat.exists

